(jbuild_version 1)

(executables
  ((names (game))
  (libraries (ppx_jane
              ppx_deriving
              ppx_sexp_conv
              sexplib
              core
  ))
  (preprocess (pps (ppx_sexp_conv ppx_deriving)))
))
